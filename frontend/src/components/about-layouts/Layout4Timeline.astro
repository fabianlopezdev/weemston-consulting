---
/**
 * Layout 4: Timeline Journey
 * Chronological visual journey with all 11 images and exact copy
 */
---

<div class="layout-timeline">
  <!-- Hero Section -->
  <section class="timeline-hero">
    <div class="container">
      <div class="hero-content">
        <img
          src="/images/about/jessica-headshot.webp"
          alt="Jessica Weeman"
          class="hero-portrait"
          loading="eager"
        />
        <h1>The Woman Behind Weemston Consulting</h1>
      </div>
    </div>
  </section>

  <!-- Timeline -->
  <section class="timeline-section">
    <div class="container">
      <div class="timeline">
        <!-- Progress Line -->
        <div class="timeline-track">
          <div class="timeline-progress"></div>
        </div>

        <!-- Milestone 1: Introduction -->
        <article class="milestone">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <div class="milestone-content">
            <p class="lead">
              Jessica Weeman is the founder and lead consultant of Weemston
              Consulting. She is an operational strategist with more than
              fifteen years of experience helping businesses and organizations
              improve culture, optimize operations, and navigate change.
            </p>
          </div>
          <figure class="milestone-image">
            <img
              src="/images/about/pulse-fund-event.webp"
              alt="Pulse Fund event"
              loading="lazy"
            />
          </figure>
        </article>

        <!-- Milestone 2: Career Span -->
        <article class="milestone milestone--alt">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <figure class="milestone-image">
            <img
              src="/images/about/fyn-team.webp"
              alt="Working with teams at Fyn"
              loading="lazy"
            />
          </figure>
          <div class="milestone-content">
            <p>
              Her career spans sectors and scales, from global nonprofits and
              early-stage startups to private equity and creative ventures.
              Jessica has led major initiatives for organizations including The
              Nature Conservancy, Earth Genome, Pulse Fund, Nonstop Enterprises,
              Fyn, and Jackson Wild, building the systems, structures, and
              experiences that move strategy into action.
            </p>
          </div>
        </article>

        <!-- Quote Milestone 1 -->
        <div class="milestone-quote">
          <div class="milestone-marker milestone-marker--quote">
            <span class="marker-dot marker-dot--accent"></span>
          </div>
          <blockquote>
            <p>
              "I believe the way people work together is just as important as
              the work itself."
            </p>
          </blockquote>
        </div>

        <!-- Milestone 3: Human-Centered Leadership -->
        <article class="milestone">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <div class="milestone-content">
            <h2>Human-Centered Leadership and Growth</h2>
            <p>
              In addition to her consulting work, Jessica is an ICF certified
              coach and the co-founder of The Authentic Collective, a coaching
              practice that helps professionals gain clarity, grow with
              intention, and navigate their work and life with greater
              confidence. This integration of coaching and consulting shapes the
              human-centered way she supports leaders, teams, and organizations.
            </p>
          </div>
          <figure class="milestone-image">
            <img
              src="/images/about/jackson-wild-event.webp"
              alt="Jackson Wild"
              loading="lazy"
            />
          </figure>
        </article>

        <!-- Milestone 4: Global Perspective -->
        <article class="milestone milestone--alt">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <figure class="milestone-image">
            <img
              src="/images/about/africa-safari.webp"
              alt="International work"
              loading="lazy"
            />
          </figure>
          <div class="milestone-content">
            <p>
              Jessica is known for her grounded, collaborative approach and her
              ability to help leaders and teams bring out their best, fostering
              trust and clarity in how they work together. She is also an avid
              traveler who has worked in more than thirty countries, bringing a
              global perspective, curiosity, and care to every engagement.
            </p>
          </div>
        </article>

        <!-- Milestone 5: Personal Glimpse -->
        <article class="milestone">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <div class="milestone-content">
            <h2>A Personal Glimpse</h2>
            <p>
              Now based in Maryland with her husband and their tiny pup Zoe,
              Jessica is often found adventuring, hosting cozy dinner parties,
              or buried in an impossibly long fantasy novel. She also loves
              creating community wherever she goes and brings a genuine warmth
              to the people around her.
            </p>
          </div>
          <div class="milestone-image-pair">
            <figure>
              <img
                src="/images/about/jessica-with-zoe.webp"
                alt="Jessica with her dog Zoe"
                loading="lazy"
              />
            </figure>
            <figure>
              <img
                src="/images/about/cowboy-hats-group.webp"
                alt="Creating community"
                loading="lazy"
              />
            </figure>
          </div>
        </article>

        <!-- Section Break -->
        <div class="timeline-break">
          <div class="break-line"></div>
          <div class="break-line"></div>
        </div>

        <!-- Section 2 Header -->
        <article class="milestone milestone--header">
          <div class="milestone-marker">
            <span class="marker-dot marker-dot--large"></span>
          </div>
          <div class="milestone-content milestone-content--header">
            <h1>How Weemston Consulting Came to Life</h1>
          </div>
        </article>

        <!-- Milestone 6: A Life Built With Intention -->
        <article class="milestone milestone--alt">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <figure class="milestone-image">
            <img
              src="/images/about/sailing-helm.webp"
              alt="Jessica sailing"
              loading="lazy"
            />
          </figure>
          <div class="milestone-content">
            <h2>A Life Built With Intention</h2>
            <p>
              Weemston began in a moment of choosing a different kind of life.
              After years leading operational and strategic work across
              nonprofits, startups, and mission-driven organizations, my husband
              and I decided to move onto a sailboat full-time. It was a shift
              rooted in values, curiosity, and the desire to build a life with
              more freedom, purpose, and intention.
            </p>
          </div>
        </article>

        <!-- Quote Milestone 2 -->
        <div class="milestone-quote">
          <div class="milestone-marker milestone-marker--quote">
            <span class="marker-dot marker-dot--primary"></span>
          </div>
          <blockquote>
            <p>
              "Weemston began as an act of choosing a life built with
              intention."
            </p>
          </blockquote>
        </div>

        <!-- Image Strip: Sailing Life -->
        <div class="image-strip">
          <figure>
            <img
              src="/images/about/sailing-gear.webp"
              alt="Life on the boat"
              loading="lazy"
            />
          </figure>
          <figure>
            <img
              src="/images/about/couple-marina.webp"
              alt="Partnership at the marina"
              loading="lazy"
            />
          </figure>
          <figure>
            <img
              src="/images/about/river-mountains.webp"
              alt="International travel"
              loading="lazy"
            />
          </figure>
        </div>

        <!-- Milestone 7: A Practice Rooted in Relationship -->
        <article class="milestone">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <div class="milestone-content milestone-content--full">
            <h2>A Practice Rooted in Relationship</h2>
            <p>
              As we navigated that transition, former colleagues and leaders I
              had partnered with began reaching out with projects that aligned
              with the work I love most. What began as a handful of meaningful
              engagements grew into something larger, shaped by relationships,
              trust, and the opportunity to bring the full range of my strengths
              into each project.
            </p>
          </div>
        </article>

        <!-- Milestone 8: The Name -->
        <article class="milestone">
          <div class="milestone-marker">
            <span class="marker-dot"></span>
          </div>
          <div class="milestone-content milestone-content--full">
            <h2>The Meaning Behind the Name</h2>
            <p>
              The name Weemston is a small nod to that foundation. Formed by
              blending my last name with my husband's, it reflects the spirit of
              building something with intention, rooted in partnership and
              shaped by a shared vision of a life and career built with care.
            </p>
          </div>
        </article>

        <!-- Final Milestone -->
        <article class="milestone milestone--final">
          <div class="milestone-marker">
            <span class="marker-dot marker-dot--large"></span>
          </div>
          <div class="milestone-content milestone-content--centered">
            <h2>A Home for Aligned, Impactful Work</h2>
            <p>
              Over time, the practice that emerged became Weemston Consulting, a
              home for work that feels aligned, meaningful, and deeply human.
            </p>
          </div>
        </article>
      </div>
    </div>
  </section>
</div>

<style>
  .layout-timeline {
    --timeline-color: var(--color-border);
    --timeline-active: var(--color-primary);
  }

  .container {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }

  /* Hero */
  .timeline-hero {
    padding: var(--space-3xl) 0;
    text-align: center;
    background: linear-gradient(
      to bottom,
      var(--color-surface),
      var(--color-background)
    );
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-lg);
  }

  .hero-portrait {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid var(--color-background);
    box-shadow: var(--shadow-lg);
  }

  .timeline-hero h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0;
    line-height: var(--line-height-tight);
  }

  /* Timeline Section */
  .timeline-section {
    padding: var(--space-2xl) 0 var(--space-3xl);
  }

  .timeline {
    position: relative;
    max-width: 900px;
    margin-inline: auto;
  }

  /* Timeline Track */
  .timeline-track {
    position: absolute;
    left: 20px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--timeline-color);
  }

  @media (min-width: 768px) {
    .timeline-track {
      left: 50%;
      transform: translateX(-50%);
    }
  }

  .timeline-progress {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 0;
    background: var(--timeline-active);
    transition: height 0.1s linear;
  }

  /* Milestone */
  .milestone {
    position: relative;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-lg);
    padding-left: 50px;
    margin-bottom: var(--space-2xl);
  }

  @media (min-width: 768px) {
    .milestone {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-xl);
      padding-left: 0;
    }

    .milestone--alt .milestone-content {
      order: 2;
    }

    .milestone--alt .milestone-image {
      order: 1;
    }
  }

  .milestone--final {
    margin-bottom: 0;
  }

  .milestone--header {
    margin-bottom: var(--space-xl);
  }

  /* Milestone Marker */
  .milestone-marker {
    position: absolute;
    left: 12px;
    top: 0;
    z-index: 2;
  }

  @media (min-width: 768px) {
    .milestone-marker {
      left: 50%;
      transform: translateX(-50%);
    }
  }

  .marker-dot {
    display: block;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--color-background);
    border: 3px solid var(--timeline-color);
    transition:
      border-color 0.3s ease,
      transform 0.3s ease;
  }

  .milestone:hover .marker-dot {
    border-color: var(--timeline-active);
    transform: scale(1.1);
  }

  .marker-dot--accent {
    border-color: var(--color-accent);
    background: var(--color-accent);
  }

  .marker-dot--primary {
    border-color: var(--color-primary);
    background: var(--color-primary);
  }

  .marker-dot--large {
    width: 24px;
    height: 24px;
    border-color: var(--timeline-active);
    background: var(--timeline-active);
  }

  /* Milestone Content */
  .milestone-content {
    padding-top: var(--space-xs);
  }

  @media (min-width: 768px) {
    .milestone-content {
      text-align: right;
      padding-right: var(--space-xl);
    }

    .milestone--alt .milestone-content {
      text-align: left;
      padding-right: 0;
      padding-left: var(--space-xl);
    }
  }

  .milestone-content--header {
    grid-column: 1 / -1;
    text-align: center;
    padding: 0;
  }

  @media (min-width: 768px) {
    .milestone-content--header {
      padding-right: 0;
    }
  }

  .milestone-content--header h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0;
  }

  .milestone-content--full {
    grid-column: 1 / -1;
    text-align: center;
    max-width: 650px;
    margin-inline: auto;
    padding: 0 var(--space-md);
  }

  @media (min-width: 768px) {
    .milestone-content--full {
      padding-right: 0;
      padding-left: 0;
    }
  }

  .milestone-content--centered {
    grid-column: 1 / -1;
    text-align: center;
    padding: var(--space-xl);
    background: var(--color-surface);
    border-radius: var(--border-radius-lg);
    max-width: 600px;
    margin-inline: auto;
  }

  @media (min-width: 768px) {
    .milestone-content--centered {
      padding-right: var(--space-xl);
      padding-left: var(--space-xl);
    }
  }

  .milestone-content h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-sm);
  }

  .milestone-content p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0;
  }

  .milestone-content .lead {
    font-size: var(--font-size-lg);
    color: var(--color-text);
  }

  /* Milestone Image */
  .milestone-image {
    margin: 0;
  }

  @media (min-width: 768px) {
    .milestone-image {
      padding-left: var(--space-xl);
    }

    .milestone--alt .milestone-image {
      padding-left: 0;
      padding-right: var(--space-xl);
    }
  }

  .milestone-image img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    border-radius: var(--border-radius-md);
    box-shadow: var(--shadow-md);
  }

  /* Milestone Image Pair */
  .milestone-image-pair {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-sm);
  }

  @media (min-width: 768px) {
    .milestone-image-pair {
      padding-left: var(--space-xl);
    }
  }

  .milestone-image-pair figure {
    margin: 0;
  }

  .milestone-image-pair img {
    width: 100%;
    aspect-ratio: 1;
    object-fit: cover;
    border-radius: var(--border-radius-md);
    box-shadow: var(--shadow-md);
  }

  /* Quote Milestone */
  .milestone-quote {
    position: relative;
    padding-left: 50px;
    margin: var(--space-xl) 0;
  }

  @media (min-width: 768px) {
    .milestone-quote {
      padding-left: 0;
      text-align: center;
    }
  }

  .milestone-quote blockquote {
    margin: 0;
    padding: var(--space-lg);
    background: var(--color-surface);
    border-radius: var(--border-radius-lg);
    max-width: 550px;
    margin-inline: auto;
  }

  .milestone-quote blockquote p {
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    font-style: italic;
    color: var(--color-text);
    margin: 0;
    line-height: var(--line-height-normal);
  }

  /* Image Strip */
  .image-strip {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-sm);
    margin: var(--space-xl) 0;
    padding-left: 50px;
  }

  @media (min-width: 768px) {
    .image-strip {
      padding-left: 0;
      gap: var(--space-md);
    }
  }

  .image-strip figure {
    margin: 0;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  .image-strip img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
  }

  /* Timeline Break */
  .timeline-break {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-md);
    padding: var(--space-xl) 0;
    margin-left: 50px;
  }

  @media (min-width: 768px) {
    .timeline-break {
      margin-left: 0;
    }
  }

  .break-line {
    height: 1px;
    width: 80px;
    background: var(--color-border);
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersReducedMotion) {
    // Hero entrance
    gsap.from('.hero-portrait', {
      opacity: 0,
      scale: 0.8,
      duration: 0.8,
      ease: 'back.out(1.5)',
    });

    gsap.from('.timeline-hero h1', {
      opacity: 0,
      y: 20,
      duration: 0.6,
      delay: 0.2,
      ease: 'power2.out',
    });

    // Timeline progress
    gsap.to('.timeline-progress', {
      height: '100%',
      ease: 'none',
      scrollTrigger: {
        trigger: '.timeline',
        start: 'top 60%',
        end: 'bottom 60%',
        scrub: true,
      },
    });

    // Milestones
    const milestones = gsap.utils.toArray('.milestone') as HTMLElement[];
    milestones.forEach((milestone) => {
      const content = milestone.querySelector('.milestone-content');
      const image = milestone.querySelector('.milestone-image');
      const imagePair = milestone.querySelector('.milestone-image-pair');
      const marker = milestone.querySelector('.marker-dot');

      if (marker) {
        gsap.from(marker, {
          scrollTrigger: {
            trigger: milestone,
            start: 'top 70%',
            once: true,
          },
          scale: 0,
          duration: 0.4,
          ease: 'back.out(2)',
        });
      }

      if (content) {
        gsap.from(content.children, {
          scrollTrigger: {
            trigger: milestone,
            start: 'top 75%',
            once: true,
          },
          opacity: 0,
          y: 30,
          duration: 0.5,
          stagger: 0.1,
          ease: 'power2.out',
        });
      }

      if (image) {
        gsap.from(image, {
          scrollTrigger: {
            trigger: milestone,
            start: 'top 75%',
            once: true,
          },
          opacity: 0,
          x: milestone.classList.contains('milestone--alt') ? -30 : 30,
          duration: 0.6,
          ease: 'power2.out',
        });
      }

      if (imagePair) {
        gsap.from(imagePair.querySelectorAll('figure'), {
          scrollTrigger: {
            trigger: milestone,
            start: 'top 75%',
            once: true,
          },
          opacity: 0,
          y: 20,
          duration: 0.5,
          stagger: 0.1,
          ease: 'power2.out',
        });
      }
    });

    // Quote milestones
    const milestoneQuotes = gsap.utils.toArray(
      '.milestone-quote'
    ) as HTMLElement[];
    milestoneQuotes.forEach((quote) => {
      gsap.from(quote.querySelector('blockquote'), {
        scrollTrigger: {
          trigger: quote,
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        y: 20,
        duration: 0.6,
        ease: 'power2.out',
      });
    });

    // Image strip
    const imageStrips = gsap.utils.toArray('.image-strip') as HTMLElement[];
    imageStrips.forEach((strip) => {
      gsap.from(strip.querySelectorAll('figure'), {
        scrollTrigger: {
          trigger: strip,
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        y: 20,
        duration: 0.5,
        stagger: 0.1,
        ease: 'power2.out',
      });
    });

    // Timeline break
    gsap.from('.timeline-break', {
      scrollTrigger: {
        trigger: '.timeline-break',
        start: 'top 80%',
        once: true,
      },
      opacity: 0,
      duration: 0.5,
      ease: 'power2.out',
    });
  }
</script>
