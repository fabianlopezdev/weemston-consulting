---
/**
 * Layout 5: Immersive Gallery
 * Image-dominant, photography-forward with all 11 images and exact copy
 */
---

<div class="layout-gallery">
  <!-- Full-Bleed Hero -->
  <section class="gallery-hero">
    <div class="hero-image-container">
      <img
        src="/images/about/jessica-headshot.webp"
        alt="Jessica Weeman"
        class="hero-image"
        loading="eager"
      />
    </div>
    <div class="hero-overlay">
      <div class="hero-content">
        <h1>The Woman Behind Weemston Consulting</h1>
      </div>
    </div>
  </section>

  <!-- Intro Strip -->
  <section class="text-strip">
    <div class="container">
      <p class="intro-text">
        Jessica Weeman is the founder and lead consultant of Weemston
        Consulting. She is an operational strategist with more than fifteen
        years of experience helping businesses and organizations improve
        culture, optimize operations, and navigate change.
      </p>
    </div>
  </section>

  <!-- Professional Gallery Strip -->
  <section class="gallery-strip">
    <div class="strip-scroll">
      <figure class="strip-item">
        <img
          src="/images/about/fyn-team.webp"
          alt="Working with teams at Fyn"
          loading="lazy"
        />
      </figure>
      <figure class="strip-item">
        <img
          src="/images/about/pulse-fund-event.webp"
          alt="Pulse Fund event"
          loading="lazy"
        />
      </figure>
      <figure class="strip-item">
        <img
          src="/images/about/jackson-wild-event.webp"
          alt="Jackson Wild"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- Career Text -->
  <section class="text-strip">
    <div class="container">
      <p>
        Her career spans sectors and scales, from global nonprofits and
        early-stage startups to private equity and creative ventures. Jessica
        has led major initiatives for organizations including The Nature
        Conservancy, Earth Genome, Pulse Fund, Nonstop Enterprises, Fyn, and
        Jackson Wild, building the systems, structures, and experiences that
        move strategy into action.
      </p>
    </div>
  </section>

  <!-- Large Quote -->
  <section class="quote-section">
    <div class="container">
      <blockquote class="large-quote">
        <p>
          "I believe the way people work together is just as important as the
          work itself."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Full-Width Showcase -->
  <section class="showcase-section">
    <div class="showcase-image">
      <img
        src="/images/about/africa-safari.webp"
        alt="International work"
        loading="lazy"
      />
    </div>
    <div class="showcase-content">
      <h2>Human-Centered Leadership and Growth</h2>
      <p>
        In addition to her consulting work, Jessica is an ICF certified coach
        and the co-founder of The Authentic Collective, a coaching practice that
        helps professionals gain clarity, grow with intention, and navigate
        their work and life with greater confidence. This integration of
        coaching and consulting shapes the human-centered way she supports
        leaders, teams, and organizations.
      </p>
    </div>
  </section>

  <!-- Global Perspective Showcase -->
  <section class="showcase-section showcase-section--reverse">
    <div class="showcase-image">
      <img
        src="/images/about/river-mountains.webp"
        alt="International travel"
        loading="lazy"
      />
    </div>
    <div class="showcase-content">
      <p>
        Jessica is known for her grounded, collaborative approach and her
        ability to help leaders and teams bring out their best, fostering trust
        and clarity in how they work together. She is also an avid traveler who
        has worked in more than thirty countries, bringing a global perspective,
        curiosity, and care to every engagement.
      </p>
    </div>
  </section>

  <!-- Personal Gallery Strip -->
  <section class="gallery-strip gallery-strip--personal">
    <div class="strip-scroll strip-scroll--reverse">
      <figure class="strip-item strip-item--portrait">
        <img
          src="/images/about/jessica-with-zoe.webp"
          alt="Jessica with her dog Zoe"
          loading="lazy"
        />
      </figure>
      <figure class="strip-item">
        <img
          src="/images/about/cowboy-hats-group.webp"
          alt="Creating community"
          loading="lazy"
        />
      </figure>
    </div>
    <div class="strip-caption">
      <h2>A Personal Glimpse</h2>
      <p>
        Now based in Maryland with her husband and their tiny pup Zoe, Jessica
        is often found adventuring, hosting cozy dinner parties, or buried in an
        impossibly long fantasy novel. She also loves creating community
        wherever she goes and brings a genuine warmth to the people around her.
      </p>
    </div>
  </section>

  <!-- Origin Section -->
  <section class="origin-section">
    <div class="origin-bg">
      <img
        src="/images/about/sailing-helm.webp"
        alt="Jessica sailing"
        loading="lazy"
      />
    </div>
    <div class="origin-overlay">
      <div class="origin-content">
        <h1>How Weemston Consulting Came to Life</h1>
        <h2>A Life Built With Intention</h2>
        <p>
          Weemston began in a moment of choosing a different kind of life. After
          years leading operational and strategic work across nonprofits,
          startups, and mission-driven organizations, my husband and I decided
          to move onto a sailboat full-time. It was a shift rooted in values,
          curiosity, and the desire to build a life with more freedom, purpose,
          and intention.
        </p>
      </div>
    </div>
  </section>

  <!-- Large Quote 2 -->
  <section class="quote-section quote-section--accent">
    <div class="container">
      <blockquote class="large-quote">
        <p>
          "Weemston began as an act of choosing a life built with intention."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Sailing Gallery -->
  <section class="dual-showcase">
    <figure class="dual-item">
      <img
        src="/images/about/sailing-gear.webp"
        alt="Life on the boat"
        loading="lazy"
      />
    </figure>
    <figure class="dual-item">
      <img
        src="/images/about/couple-marina.webp"
        alt="Partnership at the marina"
        loading="lazy"
      />
    </figure>
  </section>

  <!-- Relationship Story -->
  <section class="text-strip">
    <div class="container">
      <h2>A Practice Rooted in Relationship</h2>
      <p>
        As we navigated that transition, former colleagues and leaders I had
        partnered with began reaching out with projects that aligned with the
        work I love most. What began as a handful of meaningful engagements grew
        into something larger, shaped by relationships, trust, and the
        opportunity to bring the full range of my strengths into each project.
      </p>
    </div>
  </section>

  <!-- Name Story -->
  <section class="text-strip text-strip--muted">
    <div class="container">
      <h2>The Meaning Behind the Name</h2>
      <p>
        The name Weemston is a small nod to that foundation. Formed by blending
        my last name with my husband's, it reflects the spirit of building
        something with intention, rooted in partnership and shaped by a shared
        vision of a life and career built with care.
      </p>
    </div>
  </section>

  <!-- Closing Hero -->
  <section class="closing-hero">
    <div class="closing-content">
      <h2>A Home for Aligned, Impactful Work</h2>
      <p>
        Over time, the practice that emerged became Weemston Consulting, a home
        for work that feels aligned, meaningful, and deeply human.
      </p>
    </div>
  </section>
</div>

<style>
  .layout-gallery {
    --overlay-dark: rgba(0, 0, 0, 0.5);
    --overlay-gradient: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.7),
      transparent
    );
  }

  .container {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }

  /* Full-Bleed Hero */
  .gallery-hero {
    position: relative;
    height: 70vh;
    min-height: 500px;
    max-height: 800px;
    overflow: hidden;
  }

  .hero-image-container {
    position: absolute;
    inset: 0;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: var(--overlay-gradient);
    display: flex;
    align-items: flex-end;
    padding: var(--space-2xl);
  }

  .hero-content {
    color: var(--color-text-contrast);
  }

  .hero-content h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    margin: 0;
    line-height: var(--line-height-tight);
  }

  @media (min-width: 768px) {
    .hero-content h1 {
      font-size: var(--font-size-4xl);
    }
  }

  /* Text Strip */
  .text-strip {
    padding: var(--space-2xl) 0;
    text-align: center;
  }

  .text-strip--muted {
    background: var(--color-surface);
  }

  .text-strip h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .text-strip p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    max-width: 700px;
    margin: 0 auto;
  }

  .intro-text {
    font-size: var(--font-size-lg);
    color: var(--color-text);
  }

  /* Gallery Strip */
  .gallery-strip {
    padding: var(--space-lg) 0;
    overflow: hidden;
  }

  .gallery-strip--personal {
    background: var(--color-surface);
    padding: var(--space-xl) 0;
  }

  .strip-scroll {
    display: flex;
    gap: var(--space-md);
    padding: 0 var(--container-padding);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .strip-scroll::-webkit-scrollbar {
    display: none;
  }

  .strip-item {
    flex: 0 0 auto;
    width: 300px;
    margin: 0;
    scroll-snap-align: start;
    position: relative;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  @media (min-width: 768px) {
    .strip-item {
      width: 400px;
    }
  }

  .strip-item--portrait {
    width: 250px;
  }

  @media (min-width: 768px) {
    .strip-item--portrait {
      width: 320px;
    }
  }

  .strip-item img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
    transition: transform 0.6s ease;
  }

  .strip-item--portrait img {
    aspect-ratio: 3/4;
  }

  .strip-item:hover img {
    transform: scale(1.05);
  }

  .strip-caption {
    text-align: center;
    padding: var(--space-lg) var(--container-padding) 0;
    max-width: 700px;
    margin-inline: auto;
  }

  .strip-caption h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .strip-caption p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0;
  }

  /* Large Quote */
  .quote-section {
    padding: var(--space-3xl) 0;
    background: var(--color-surface);
  }

  .quote-section--accent {
    background: color-mix(in srgb, var(--color-accent) 10%, white);
  }

  .large-quote {
    text-align: center;
    margin: 0;
  }

  .large-quote p {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-style: italic;
    color: var(--color-text);
    margin: 0;
    max-width: 700px;
    margin-inline: auto;
    line-height: var(--line-height-normal);
  }

  @media (min-width: 768px) {
    .large-quote p {
      font-size: var(--font-size-3xl);
    }
  }

  /* Showcase Section */
  .showcase-section {
    display: grid;
    grid-template-columns: 1fr;
    min-height: 400px;
  }

  @media (min-width: 768px) {
    .showcase-section {
      grid-template-columns: 1fr 1fr;
      min-height: 500px;
    }

    .showcase-section--reverse {
      grid-template-columns: 1fr 1fr;
    }

    .showcase-section--reverse .showcase-image {
      order: 2;
    }

    .showcase-section--reverse .showcase-content {
      order: 1;
    }
  }

  .showcase-image {
    position: relative;
    overflow: hidden;
  }

  .showcase-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    min-height: 300px;
  }

  .showcase-content {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-xl);
    background: var(--color-surface);
  }

  .showcase-content h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .showcase-content p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0;
  }

  /* Origin Section */
  .origin-section {
    position: relative;
    min-height: 600px;
    display: flex;
    align-items: center;
  }

  .origin-bg {
    position: absolute;
    inset: 0;
  }

  .origin-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .origin-overlay {
    position: absolute;
    inset: 0;
    background: var(--overlay-dark);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-2xl);
  }

  .origin-content {
    text-align: center;
    color: var(--color-text-contrast);
    max-width: 700px;
  }

  .origin-content h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    margin: 0 0 var(--space-lg);
  }

  @media (min-width: 768px) {
    .origin-content h1 {
      font-size: var(--font-size-3xl);
    }
  }

  .origin-content h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    margin: 0 0 var(--space-md);
    opacity: 0.9;
  }

  .origin-content p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    margin: 0;
    opacity: 0.9;
  }

  @media (min-width: 768px) {
    .origin-content p {
      font-size: var(--font-size-lg);
    }
  }

  /* Dual Showcase */
  .dual-showcase {
    display: grid;
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .dual-showcase {
      grid-template-columns: 1fr 1fr;
    }
  }

  .dual-item {
    margin: 0;
    overflow: hidden;
  }

  .dual-item img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
    transition: transform 0.6s ease;
  }

  .dual-item:hover img {
    transform: scale(1.03);
  }

  /* Closing Hero */
  .closing-hero {
    background: var(--color-text);
    padding: var(--space-3xl);
    text-align: center;
  }

  .closing-content {
    max-width: 650px;
    margin-inline: auto;
  }

  .closing-content h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text-contrast);
    margin: 0 0 var(--space-md);
  }

  .closing-content p {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersReducedMotion) {
    // Hero Ken Burns effect
    gsap.to('.hero-image', {
      scale: 1.1,
      ease: 'none',
      scrollTrigger: {
        trigger: '.gallery-hero',
        start: 'top top',
        end: 'bottom top',
        scrub: true,
      },
    });

    // Hero content entrance
    gsap.from('.hero-content h1', {
      opacity: 0,
      y: 30,
      duration: 0.8,
      delay: 0.3,
      ease: 'power2.out',
    });

    // Text strips
    const textStrips = gsap.utils.toArray('.text-strip') as HTMLElement[];
    textStrips.forEach((strip) => {
      gsap.from(strip.querySelectorAll('h2, p'), {
        scrollTrigger: {
          trigger: strip,
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        y: 30,
        duration: 0.6,
        stagger: 0.1,
        ease: 'power2.out',
      });
    });

    // Gallery strips
    const stripItems = gsap.utils.toArray('.strip-item') as HTMLElement[];
    stripItems.forEach((item, i) => {
      gsap.from(item, {
        scrollTrigger: {
          trigger: item.closest('.gallery-strip'),
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        y: 40,
        duration: 0.6,
        delay: i * 0.1,
        ease: 'power2.out',
      });
    });

    // Strip caption
    const stripCaptions = gsap.utils.toArray('.strip-caption') as HTMLElement[];
    stripCaptions.forEach((caption) => {
      gsap.from(caption.children, {
        scrollTrigger: {
          trigger: caption,
          start: 'top 85%',
          once: true,
        },
        opacity: 0,
        y: 20,
        duration: 0.5,
        stagger: 0.1,
        ease: 'power2.out',
      });
    });

    // Large quotes
    const largeQuotes = gsap.utils.toArray('.large-quote') as HTMLElement[];
    largeQuotes.forEach((quote) => {
      gsap.from(quote, {
        scrollTrigger: {
          trigger: quote,
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        scale: 0.95,
        duration: 0.8,
        ease: 'power2.out',
      });
    });

    // Showcase images - Ken Burns
    const showcaseImages = gsap.utils.toArray(
      '.showcase-image img'
    ) as HTMLElement[];
    showcaseImages.forEach((img) => {
      gsap.to(img, {
        scale: 1.08,
        ease: 'none',
        scrollTrigger: {
          trigger: img,
          start: 'top bottom',
          end: 'bottom top',
          scrub: true,
        },
      });
    });

    // Showcase content
    const showcaseContents = gsap.utils.toArray(
      '.showcase-content'
    ) as HTMLElement[];
    showcaseContents.forEach((content) => {
      gsap.from(content.children, {
        scrollTrigger: {
          trigger: content,
          start: 'top 75%',
          once: true,
        },
        opacity: 0,
        y: 30,
        duration: 0.6,
        stagger: 0.1,
        ease: 'power2.out',
      });
    });

    // Origin section - parallax
    gsap.to('.origin-bg img', {
      yPercent: 20,
      ease: 'none',
      scrollTrigger: {
        trigger: '.origin-section',
        start: 'top bottom',
        end: 'bottom top',
        scrub: true,
      },
    });

    gsap.from('.origin-content > *', {
      scrollTrigger: {
        trigger: '.origin-section',
        start: 'top 60%',
        once: true,
      },
      opacity: 0,
      y: 30,
      duration: 0.7,
      stagger: 0.1,
      ease: 'power2.out',
    });

    // Dual showcase
    const dualItems = gsap.utils.toArray('.dual-item') as HTMLElement[];
    dualItems.forEach((item, i) => {
      gsap.from(item, {
        scrollTrigger: {
          trigger: item,
          start: 'top 85%',
          once: true,
        },
        opacity: 0,
        y: 40,
        duration: 0.6,
        delay: i * 0.15,
        ease: 'power2.out',
      });
    });

    // Closing
    gsap.from('.closing-content > *', {
      scrollTrigger: {
        trigger: '.closing-hero',
        start: 'top 80%',
        once: true,
      },
      opacity: 0,
      y: 20,
      duration: 0.6,
      stagger: 0.1,
      ease: 'power2.out',
    });
  }
</script>
