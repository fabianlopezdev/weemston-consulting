---
/**
 * Layout 3: Image Collage Narrative
 * Rich visual storytelling with grouped images
 * - Full-width hero band with headshot + text overlay
 * - 2-3 image collages between text sections
 * - Images grouped by theme (professional, sailing, personal)
 * - Pull quotes overlaid on muted image backgrounds
 * - Asymmetric grid layouts
 */
---

<div class="layout-collage">
  <!-- Hero Band -->
  <section class="hero-band">
    <div class="hero-content">
      <div class="hero-portrait">
        <img
          src="/images/about/jessica-headshot.webp"
          alt="Jessica Weeman"
          loading="eager"
        />
      </div>
      <div class="hero-text">
        <h1>The Woman Behind Weemston Consulting</h1>
      </div>
    </div>
  </section>

  <!-- Intro Section -->
  <section class="text-section">
    <div class="container">
      <div class="text-content text-content--wide">
        <p class="lead">
          Jessica Weeman is the founder and lead consultant of Weemston
          Consulting. She is an operational strategist with more than fifteen
          years of experience helping businesses and organizations improve
          culture, optimize operations, and navigate change.
        </p>
        <p>
          Her career spans sectors and scales, from global nonprofits and
          early-stage startups to private equity and creative ventures. Jessica
          has led major initiatives for organizations including The Nature
          Conservancy, Earth Genome, Pulse Fund, Nonstop Enterprises, Fyn, and
          Jackson Wild, building the systems, structures, and experiences that
          move strategy into action.
        </p>
      </div>
    </div>
  </section>

  <!-- Professional Collage -->
  <section class="collage-section">
    <div class="collage-grid collage-grid--professional">
      <figure class="collage-item collage-item--large">
        <img
          src="/images/about/fyn-team.webp"
          alt="Jessica with the Fyn team"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--tall">
        <img
          src="/images/about/pulse-fund-event.webp"
          alt="Networking at Pulse Fund"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--wide">
        <img
          src="/images/about/jackson-wild-event.webp"
          alt="Jackson Wild event"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- Quote on Image Background -->
  <section class="quote-overlay-section">
    <div class="quote-bg">
      <img
        src="/images/about/river-mountains.webp"
        alt="Background"
        loading="lazy"
      />
    </div>
    <div class="quote-overlay-content">
      <blockquote>
        <p>
          "I believe the way people work together is just as important as the
          work itself."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Leadership Section -->
  <section class="text-section">
    <div class="container">
      <div class="text-content">
        <h2>Human-Centered Leadership and Growth</h2>
        <p>
          In addition to her consulting work, Jessica is an ICF certified coach
          and the co-founder of The Authentic Collective, a coaching practice
          that helps professionals gain clarity, grow with intention, and
          navigate their work and life with greater confidence. This integration
          of coaching and consulting shapes the human-centered way she supports
          leaders, teams, and organizations.
        </p>
        <p>
          Jessica is known for her grounded, collaborative approach and her
          ability to help leaders and teams bring out their best, fostering
          trust and clarity in how they work together. She is also an avid
          traveler who has worked in more than thirty countries, bringing a
          global perspective, curiosity, and care to every engagement.
        </p>
      </div>
    </div>
  </section>

  <!-- Travel & Personal Collage -->
  <section class="collage-section">
    <div class="collage-grid collage-grid--personal">
      <figure class="collage-item collage-item--featured">
        <img
          src="/images/about/africa-safari.webp"
          alt="Safari in Africa"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--square">
        <img
          src="/images/about/jessica-with-zoe.webp"
          alt="Jessica with Zoe"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--small">
        <img
          src="/images/about/cowboy-hats-group.webp"
          alt="Fun with friends"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- Personal Glimpse -->
  <section class="text-section text-section--muted">
    <div class="container">
      <div class="text-content text-content--centered">
        <h2>A Personal Glimpse</h2>
        <p>
          Now based in Maryland with her husband and their tiny pup Zoe, Jessica
          is often found adventuring, hosting cozy dinner parties, or buried in
          an impossibly long fantasy novel. She also loves creating community
          wherever she goes and brings a genuine warmth to the people around
          her.
        </p>
      </div>
    </div>
  </section>

  <!-- Section Divider -->
  <div class="divider">
    <span></span>
  </div>

  <!-- Origin Story -->
  <section class="text-section">
    <div class="container">
      <div class="text-content text-content--wide">
        <h1>How Weemston Consulting Came to Life</h1>
        <h3>A Life Built With Intention</h3>
        <p>
          Weemston began in a moment of choosing a different kind of life. After
          years leading operational and strategic work across nonprofits,
          startups, and mission-driven organizations, my husband and I decided
          to move onto a sailboat full-time. It was a shift rooted in values,
          curiosity, and the desire to build a life with more freedom, purpose,
          and intention.
        </p>
      </div>
    </div>
  </section>

  <!-- Sailing Collage -->
  <section class="collage-section">
    <div class="collage-grid collage-grid--sailing">
      <figure class="collage-item collage-item--hero">
        <img
          src="/images/about/sailing-helm.webp"
          alt="Jessica at the helm"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--side">
        <img
          src="/images/about/sailing-gear.webp"
          alt="Life on the boat"
          loading="lazy"
        />
      </figure>
      <figure class="collage-item collage-item--bottom">
        <img
          src="/images/about/couple-marina.webp"
          alt="At the marina with her husband"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- Quote Band -->
  <section class="quote-band">
    <div class="container">
      <blockquote>
        <p>
          "Weemston began as an act of choosing a life built with intention."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Closing Content -->
  <section class="text-section">
    <div class="container">
      <div class="text-content">
        <h3>A Practice Rooted in Relationship</h3>
        <p>
          As we navigated that transition, former colleagues and leaders I had
          partnered with began reaching out with projects that aligned with the
          work I love most. What began as a handful of meaningful engagements
          grew into something larger, shaped by relationships, trust, and the
          opportunity to bring the full range of my strengths into each project.
        </p>

        <h3>The Meaning Behind the Name</h3>
        <p>
          The name Weemston is a small nod to that foundation. Formed by
          blending my last name with my husband's, it reflects the spirit of
          building something with intention, rooted in partnership and shaped by
          a shared vision of a life and career built with care.
        </p>
      </div>
    </div>
  </section>

  <!-- Closing Statement -->
  <section class="closing-statement">
    <div class="container">
      <h2>A Home for Aligned, Impactful Work</h2>
      <p>
        Over time, the practice that emerged became Weemston Consulting, a home
        for work that feels aligned, meaningful, and deeply human.
      </p>
    </div>
  </section>
</div>

<style>
  .layout-collage {
    --section-gap: var(--space-2xl);
  }

  .container {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }

  /* Hero Band */
  .hero-band {
    background: var(--color-text);
    padding: var(--space-2xl) 0;
  }

  .hero-content {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    align-items: center;
    text-align: center;
  }

  @media (min-width: 768px) {
    .hero-content {
      flex-direction: row;
      justify-content: center;
      gap: var(--space-2xl);
    }
  }

  .hero-portrait {
    flex-shrink: 0;
  }

  .hero-portrait img {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid rgba(255, 255, 255, 0.2);
  }

  @media (min-width: 768px) {
    .hero-portrait img {
      width: 200px;
      height: 200px;
    }
  }

  .hero-eyebrow {
    display: block;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-primary-light);
    margin-bottom: var(--space-sm);
  }

  .hero-text h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text-contrast);
    margin: 0 0 var(--space-sm);
    line-height: var(--line-height-tight);
  }

  .hero-text p {
    font-size: var(--font-size-base);
    color: rgba(255, 255, 255, 0.7);
    margin: 0;
    max-width: 500px;
  }

  /* Text Sections */
  .text-section {
    padding: var(--section-gap) 0;
  }

  .text-section--muted {
    background: var(--color-surface);
  }

  .text-content {
    max-width: 650px;
    margin-inline: auto;
  }

  .text-content--wide {
    max-width: 750px;
  }

  .text-content--centered {
    text-align: center;
  }

  .text-content h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-lg);
  }

  .text-content h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .text-content h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: var(--space-xl) 0 var(--space-md);
  }

  .text-content h3:first-child {
    margin-top: 0;
  }

  .text-content p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0 0 var(--space-md);
  }

  .text-content p:last-child {
    margin-bottom: 0;
  }

  .text-content .lead {
    font-size: var(--font-size-lg);
    color: var(--color-text);
  }

  /* Collage Sections */
  .collage-section {
    padding: var(--space-lg) var(--container-padding);
  }

  .collage-grid {
    display: grid;
    gap: var(--space-sm);
    max-width: 1100px;
    margin-inline: auto;
  }

  .collage-item {
    margin: 0;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  .collage-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }

  .collage-item:hover img {
    transform: scale(1.03);
  }

  /* Professional Grid */
  .collage-grid--professional {
    grid-template-columns: 1fr;
    grid-template-rows: auto;
  }

  @media (min-width: 768px) {
    .collage-grid--professional {
      grid-template-columns: 1.5fr 1fr;
      grid-template-rows: 200px 200px;
    }

    .collage-grid--professional .collage-item--large {
      grid-row: span 2;
    }
  }

  /* Personal Grid */
  .collage-grid--personal {
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .collage-grid--personal {
      grid-template-columns: 2fr 1fr;
      grid-template-rows: 180px 180px;
    }

    .collage-grid--personal .collage-item--featured {
      grid-row: span 2;
    }

    .collage-grid--personal .collage-item--square {
      aspect-ratio: 1;
    }
  }

  /* Sailing Grid */
  .collage-grid--sailing {
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .collage-grid--sailing {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 250px 200px;
    }

    .collage-grid--sailing .collage-item--hero {
      grid-row: span 2;
    }
  }

  /* Quote on Image */
  .quote-overlay-section {
    position: relative;
    min-height: 350px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .quote-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .quote-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.4);
  }

  .quote-overlay-content {
    position: relative;
    z-index: 1;
    padding: var(--space-2xl);
    text-align: center;
    max-width: 700px;
  }

  .quote-overlay-content blockquote {
    margin: 0;
  }

  .quote-overlay-content p {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-style: italic;
    color: var(--color-text-contrast);
    line-height: var(--line-height-normal);
    margin: 0;
  }

  /* Quote Band */
  .quote-band {
    background: color-mix(in srgb, var(--color-accent) 15%, white);
    padding: var(--space-2xl) 0;
    text-align: center;
  }

  .quote-band blockquote {
    margin: 0;
  }

  .quote-band p {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-style: italic;
    color: var(--color-text);
    margin: 0;
    max-width: 600px;
    margin-inline: auto;
  }

  /* Divider */
  .divider {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-xl) var(--container-padding);
  }

  .divider span {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-text-muted);
    padding: 0 var(--space-lg);
    position: relative;
  }

  .divider span::before,
  .divider span::after {
    content: '';
    position: absolute;
    top: 50%;
    width: 60px;
    height: 1px;
    background: var(--color-border);
  }

  .divider span::before {
    right: 100%;
  }

  .divider span::after {
    left: 100%;
  }

  /* Closing Statement */
  .closing-statement {
    background: var(--color-text);
    color: var(--color-text-contrast);
    padding: var(--space-3xl) var(--container-padding);
    text-align: center;
  }

  .closing-statement h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    margin: 0 0 var(--space-md);
  }

  .closing-statement p {
    font-size: var(--font-size-lg);
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
    max-width: 550px;
    margin-inline: auto;
    line-height: var(--line-height-relaxed);
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersReducedMotion) {
    // Hero entrance
    gsap.from('.hero-portrait', {
      opacity: 0,
      scale: 0.9,
      duration: 0.8,
      ease: 'power2.out',
    });

    gsap.from('.hero-text > *', {
      opacity: 0,
      y: 20,
      duration: 0.6,
      stagger: 0.1,
      delay: 0.2,
      ease: 'power2.out',
    });

    // Collage items
    const collageItems = gsap.utils.toArray('.collage-item') as HTMLElement[];
    collageItems.forEach((item, index) => {
      gsap.from(item, {
        scrollTrigger: {
          trigger: item,
          start: 'top 85%',
          once: true,
        },
        opacity: 0,
        y: 30,
        duration: 0.6,
        delay: index * 0.1,
        ease: 'power2.out',
      });
    });

    // Text sections
    const textContents = gsap.utils.toArray('.text-content') as HTMLElement[];
    textContents.forEach((content) => {
      gsap.from(content.children, {
        scrollTrigger: {
          trigger: content,
          start: 'top 80%',
          once: true,
        },
        opacity: 0,
        y: 30,
        duration: 0.6,
        stagger: 0.1,
        ease: 'power2.out',
      });
    });

    // Quote overlay
    gsap.from('.quote-overlay-content', {
      scrollTrigger: {
        trigger: '.quote-overlay-section',
        start: 'top 70%',
        once: true,
      },
      opacity: 0,
      y: 30,
      duration: 0.8,
      ease: 'power2.out',
    });

    // Closing statement
    gsap.from('.closing-statement > *', {
      scrollTrigger: {
        trigger: '.closing-statement',
        start: 'top 80%',
        once: true,
      },
      opacity: 0,
      y: 20,
      duration: 0.6,
      stagger: 0.1,
      ease: 'power2.out',
    });
  }
</script>
