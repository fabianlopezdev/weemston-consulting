---
/**
 * Layout 2: Two-Column Story
 * Magazine-style alternating layout with sticky sidebar
 * Uses all 11 images with exact copy
 */
---

<div class="layout-magazine">
  <section class="hero-section">
    <div class="container">
      <div class="hero-layout">
        <!-- Sticky Portrait Sidebar -->
        <aside class="portrait-sidebar">
          <div class="portrait-sticky">
            <div class="portrait-frame">
              <img
                src="/images/about/jessica-headshot.webp"
                alt="Jessica Weeman"
                loading="eager"
              />
            </div>
            <div class="portrait-info">
              <span class="portrait-name">Jessica Weeman</span>
              <span class="portrait-title"
                >Founder and Operational Strategist</span
              >
            </div>
          </div>
        </aside>

        <!-- Scrolling Content -->
        <div class="content-column">
          <!-- Section 1: The Woman Behind -->
          <article class="story-section">
            <header class="story-header">
              <h1>The Woman Behind Weemston Consulting</h1>
            </header>

            <div class="story-block">
              <p class="story-lead">
                Jessica Weeman is the founder and lead consultant of Weemston
                Consulting. She is an operational strategist with more than
                fifteen years of experience helping businesses and organizations
                improve culture, optimize operations, and navigate change.
              </p>
              <p>
                Her career spans sectors and scales, from global nonprofits and
                early-stage startups to private equity and creative ventures.
                Jessica has led major initiatives for organizations including
                The Nature Conservancy, Earth Genome, Pulse Fund, Nonstop
                Enterprises, Fyn, and Jackson Wild, building the systems,
                structures, and experiences that move strategy into action.
              </p>
            </div>

            <!-- Professional Images Grid -->
            <div class="image-grid">
              <figure class="grid-item">
                <img
                  src="/images/about/fyn-team.webp"
                  alt="Working with teams at Fyn"
                  loading="lazy"
                />
              </figure>
              <figure class="grid-item">
                <img
                  src="/images/about/jackson-wild-event.webp"
                  alt="Jackson Wild"
                  loading="lazy"
                />
              </figure>
              <figure class="grid-item">
                <img
                  src="/images/about/pulse-fund-event.webp"
                  alt="Pulse Fund event"
                  loading="lazy"
                />
              </figure>
              <figure class="grid-item">
                <img
                  src="/images/about/africa-safari.webp"
                  alt="International work"
                  loading="lazy"
                />
              </figure>
            </div>

            <!-- Pull Quote Box -->
            <blockquote class="quote-box">
              <p>
                "I believe the way people work together is just as important as
                the work itself."
              </p>
            </blockquote>

            <div class="story-block">
              <h2>Human-Centered Leadership and Growth</h2>
              <p>
                In addition to her consulting work, Jessica is an ICF certified
                coach and the co-founder of The Authentic Collective, a coaching
                practice that helps professionals gain clarity, grow with
                intention, and navigate their work and life with greater
                confidence. This integration of coaching and consulting shapes
                the human-centered way she supports leaders, teams, and
                organizations.
              </p>
              <p>
                Jessica is known for her grounded, collaborative approach and
                her ability to help leaders and teams bring out their best,
                fostering trust and clarity in how they work together. She is
                also an avid traveler who has worked in more than thirty
                countries, bringing a global perspective, curiosity, and care to
                every engagement.
              </p>
            </div>

            <figure class="story-figure">
              <img
                src="/images/about/river-mountains.webp"
                alt="International travel"
                loading="lazy"
              />
            </figure>

            <div class="story-block">
              <h2>A Personal Glimpse</h2>
              <p>
                Now based in Maryland with her husband and their tiny pup Zoe,
                Jessica is often found adventuring, hosting cozy dinner parties,
                or buried in an impossibly long fantasy novel. She also loves
                creating community wherever she goes and brings a genuine warmth
                to the people around her.
              </p>
            </div>

            <div class="image-pair">
              <figure class="pair-item">
                <img
                  src="/images/about/jessica-with-zoe.webp"
                  alt="Jessica with her dog Zoe"
                  loading="lazy"
                />
              </figure>
              <figure class="pair-item">
                <img
                  src="/images/about/cowboy-hats-group.webp"
                  alt="Creating community"
                  loading="lazy"
                />
              </figure>
            </div>
          </article>

          <!-- Timeline Connector -->
          <div class="timeline-connector">
            <div class="timeline-line"></div>
            <div class="timeline-dot"></div>
          </div>

          <!-- Section 2: Origin Story -->
          <article class="story-section">
            <header class="story-header">
              <h1>How Weemston Consulting Came to Life</h1>
            </header>

            <div class="story-block">
              <h2>A Life Built With Intention</h2>
              <p>
                Weemston began in a moment of choosing a different kind of life.
                After years leading operational and strategic work across
                nonprofits, startups, and mission-driven organizations, my
                husband and I decided to move onto a sailboat full-time. It was
                a shift rooted in values, curiosity, and the desire to build a
                life with more freedom, purpose, and intention.
              </p>
            </div>

            <figure class="story-figure">
              <img
                src="/images/about/sailing-helm.webp"
                alt="Jessica sailing"
                loading="lazy"
              />
            </figure>

            <!-- Pull Quote Box -->
            <blockquote class="quote-box quote-box--accent">
              <p>
                "Weemston began as an act of choosing a life built with
                intention."
              </p>
            </blockquote>

            <!-- Sailing Images -->
            <div class="image-pair">
              <figure class="pair-item">
                <img
                  src="/images/about/sailing-gear.webp"
                  alt="Life on the boat"
                  loading="lazy"
                />
              </figure>
              <figure class="pair-item">
                <img
                  src="/images/about/couple-marina.webp"
                  alt="Partnership at the marina"
                  loading="lazy"
                />
              </figure>
            </div>

            <div class="story-block">
              <h2>A Practice Rooted in Relationship</h2>
              <p>
                As we navigated that transition, former colleagues and leaders I
                had partnered with began reaching out with projects that aligned
                with the work I love most. What began as a handful of meaningful
                engagements grew into something larger, shaped by relationships,
                trust, and the opportunity to bring the full range of my
                strengths into each project.
              </p>
            </div>

            <div class="story-block">
              <h2>The Meaning Behind the Name</h2>
              <p>
                The name Weemston is a small nod to that foundation. Formed by
                blending my last name with my husband's, it reflects the spirit
                of building something with intention, rooted in partnership and
                shaped by a shared vision of a life and career built with care.
              </p>
            </div>

            <div class="story-block story-block--closing">
              <h2>A Home for Aligned, Impactful Work</h2>
              <p>
                Over time, the practice that emerged became Weemston Consulting,
                a home for work that feels aligned, meaningful, and deeply
                human.
              </p>
            </div>
          </article>
        </div>
      </div>
    </div>
  </section>
</div>

<style>
  .layout-magazine {
    --content-max: 680px;
    --sidebar-width: 280px;
    --gap: var(--space-2xl);
  }

  .container {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }

  .hero-section {
    padding-block: var(--space-2xl) var(--space-3xl);
  }

  .hero-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
  }

  @media (min-width: 900px) {
    .hero-layout {
      grid-template-columns: var(--sidebar-width) 1fr;
      gap: var(--gap);
    }
  }

  /* Portrait Sidebar */
  .portrait-sidebar {
    order: -1;
  }

  @media (min-width: 900px) {
    .portrait-sidebar {
      order: 0;
    }

    .portrait-sticky {
      position: sticky;
      top: calc(var(--header-height) + var(--space-lg));
    }
  }

  .portrait-frame {
    overflow: hidden;
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-lg);
  }

  .portrait-frame img {
    width: 100%;
    aspect-ratio: 4/5;
    object-fit: cover;
    display: block;
  }

  @media (max-width: 899px) {
    .portrait-frame {
      max-width: 200px;
    }
  }

  .portrait-info {
    margin-top: var(--space-md);
    text-align: center;
  }

  .portrait-name {
    display: block;
    font-family: var(--font-heading);
    font-size: var(--font-size-lg);
    color: var(--color-text);
    margin-bottom: 4px;
  }

  .portrait-title {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  /* Content Column */
  .content-column {
    max-width: var(--content-max);
  }

  /* Story Section */
  .story-section {
    margin-bottom: var(--space-2xl);
  }

  .story-header {
    margin-bottom: var(--space-xl);
  }

  .story-header h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-tight);
    color: var(--color-text);
    margin: 0;
  }

  /* Story Block */
  .story-block {
    margin-bottom: var(--space-xl);
  }

  .story-block h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .story-block p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0 0 var(--space-md);
  }

  .story-block p:last-child {
    margin-bottom: 0;
  }

  .story-lead {
    font-size: var(--font-size-lg);
    color: var(--color-text);
  }

  .story-block--closing {
    text-align: center;
    padding: var(--space-xl);
    background: var(--color-surface);
    border-radius: var(--border-radius-lg);
  }

  /* Quote Box */
  .quote-box {
    background: var(--color-surface);
    padding: var(--space-lg);
    border-radius: var(--border-radius-lg);
    border-left: 4px solid var(--color-primary);
    margin: var(--space-xl) 0;
  }

  .quote-box--accent {
    background: color-mix(in srgb, var(--color-accent) 10%, white);
    border-left-color: var(--color-accent);
  }

  .quote-box p {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-style: italic;
    line-height: var(--line-height-normal);
    color: var(--color-text);
    margin: 0;
  }

  /* Story Figure */
  .story-figure {
    margin: var(--space-xl) 0;
  }

  .story-figure img {
    width: 100%;
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-md);
  }

  /* Image Grid */
  .image-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-sm);
    margin: var(--space-xl) 0;
  }

  .grid-item {
    margin: 0;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  .grid-item img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
  }

  /* Image Pair */
  .image-pair {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    margin: var(--space-xl) 0;
  }

  @media (min-width: 500px) {
    .image-pair {
      grid-template-columns: 1fr 1fr;
    }
  }

  .pair-item {
    margin: 0;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  .pair-item img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
  }

  /* Timeline Connector */
  .timeline-connector {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--space-xl) 0;
  }

  .timeline-line {
    width: 2px;
    height: 60px;
    background: linear-gradient(
      to bottom,
      var(--color-border),
      var(--color-primary)
    );
  }

  .timeline-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-primary);
    margin-top: -1px;
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersReducedMotion) {
    gsap.from('.portrait-frame', {
      opacity: 0,
      scale: 0.95,
      duration: 0.8,
      ease: 'power2.out',
    });

    gsap.from('.portrait-info', {
      opacity: 0,
      y: 10,
      duration: 0.5,
      delay: 0.3,
      ease: 'power2.out',
    });

    gsap.from('.story-header', {
      opacity: 0,
      y: 30,
      duration: 0.6,
      delay: 0.2,
      ease: 'power2.out',
    });

    const storyBlocks = gsap.utils.toArray('.story-block') as HTMLElement[];
    storyBlocks.forEach((block) => {
      gsap.from(block, {
        scrollTrigger: { trigger: block, start: 'top 85%', once: true },
        opacity: 0,
        y: 30,
        duration: 0.6,
        ease: 'power2.out',
      });
    });

    const imageElements = gsap.utils.toArray(
      '.story-figure, .image-grid, .image-pair'
    ) as HTMLElement[];
    imageElements.forEach((el) => {
      gsap.from(el, {
        scrollTrigger: { trigger: el, start: 'top 85%', once: true },
        opacity: 0,
        y: 20,
        duration: 0.7,
        ease: 'power2.out',
      });
    });

    const quoteBoxes = gsap.utils.toArray('.quote-box') as HTMLElement[];
    quoteBoxes.forEach((quote) => {
      gsap.from(quote, {
        scrollTrigger: { trigger: quote, start: 'top 85%', once: true },
        opacity: 0,
        x: -20,
        duration: 0.6,
        ease: 'power2.out',
      });
    });
  }
</script>
