---
/**
 * Layout 1: Editorial Minimal
 * Clean, spacious, text-forward with images as accents
 * Uses all 11 images with a carousel for professional work images
 */
---

<div class="layout-editorial">
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-grid">
        <div class="hero-image">
          <img
            src="/images/about/jessica-headshot.webp"
            alt="Jessica Weeman, Founder of Weemston Consulting"
            loading="eager"
          />
        </div>
        <div class="hero-content">
          <span class="hero-label">Founder and Operational Strategist</span>
          <h1>The Woman Behind Weemston Consulting</h1>
          <p class="hero-intro">
            Jessica Weeman is the founder and lead consultant of Weemston
            Consulting. She is an operational strategist with more than fifteen
            years of experience helping businesses and organizations improve
            culture, optimize operations, and navigate change.
          </p>
          <p class="hero-detail">
            Her career spans sectors and scales, from global nonprofits and
            early-stage startups to private equity and creative ventures.
            Jessica has led major initiatives for organizations including The
            Nature Conservancy, Earth Genome, Pulse Fund, Nonstop Enterprises,
            Fyn, and Jackson Wild, building the systems, structures, and
            experiences that move strategy into action.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Professional Work Carousel -->
  <section class="carousel-section">
    <div class="carousel-track">
      <figure class="carousel-item">
        <img src="/images/about/fyn-team.webp" alt="Fyn team" loading="lazy" />
      </figure>
      <figure class="carousel-item">
        <img
          src="/images/about/jackson-wild-event.webp"
          alt="Jackson Wild"
          loading="lazy"
        />
      </figure>
      <figure class="carousel-item">
        <img
          src="/images/about/pulse-fund-event.webp"
          alt="Pulse Fund"
          loading="lazy"
        />
      </figure>
      <figure class="carousel-item">
        <img
          src="/images/about/africa-safari.webp"
          alt="International work"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- Pull Quote 1 -->
  <section class="pull-quote-section">
    <div class="container">
      <blockquote class="pull-quote">
        <p>
          "I believe the way people work together is just as important as the
          work itself."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Human-Centered Leadership -->
  <section class="content-section">
    <div class="container">
      <div class="content-grid content-grid--reverse">
        <div class="content-text">
          <h2>Human-Centered Leadership and Growth</h2>
          <p>
            In addition to her consulting work, Jessica is an ICF certified
            coach and the co-founder of The Authentic Collective, a coaching
            practice that helps professionals gain clarity, grow with intention,
            and navigate their work and life with greater confidence. This
            integration of coaching and consulting shapes the human-centered way
            she supports leaders, teams, and organizations.
          </p>
          <p>
            Jessica is known for her grounded, collaborative approach and her
            ability to help leaders and teams bring out their best, fostering
            trust and clarity in how they work together. She is also an avid
            traveler who has worked in more than thirty countries, bringing a
            global perspective, curiosity, and care to every engagement.
          </p>
        </div>
        <div class="content-image content-image--accent">
          <img
            src="/images/about/river-mountains.webp"
            alt="Jessica traveling internationally"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Personal Glimpse -->
  <section class="content-section content-section--muted">
    <div class="container">
      <div class="content-grid">
        <div class="content-image content-image--portrait">
          <img
            src="/images/about/jessica-with-zoe.webp"
            alt="Jessica with her dog Zoe"
            loading="lazy"
          />
        </div>
        <div class="content-text">
          <h2>A Personal Glimpse</h2>
          <p>
            Now based in Maryland with her husband and their tiny pup Zoe,
            Jessica is often found adventuring, hosting cozy dinner parties, or
            buried in an impossibly long fantasy novel. She also loves creating
            community wherever she goes and brings a genuine warmth to the
            people around her.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Community Image -->
  <section class="full-width-image">
    <img
      src="/images/about/cowboy-hats-group.webp"
      alt="Jessica with friends"
      loading="lazy"
    />
  </section>

  <!-- Section Divider -->
  <div class="section-divider">
    <div class="container">
      <hr />
    </div>
  </div>

  <!-- Origin Story Header -->
  <section class="origin-header">
    <div class="container">
      <h2 class="origin-title">How Weemston Consulting Came to Life</h2>
    </div>
  </section>

  <!-- A Life Built With Intention -->
  <section class="content-section">
    <div class="container">
      <div class="content-grid">
        <div class="content-text">
          <h3>A Life Built With Intention</h3>
          <p>
            Weemston began in a moment of choosing a different kind of life.
            After years leading operational and strategic work across
            nonprofits, startups, and mission-driven organizations, my husband
            and I decided to move onto a sailboat full-time. It was a shift
            rooted in values, curiosity, and the desire to build a life with
            more freedom, purpose, and intention.
          </p>
        </div>
        <div class="content-image content-image--sailing">
          <img
            src="/images/about/sailing-helm.webp"
            alt="Jessica sailing"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Pull Quote 2 -->
  <section class="pull-quote-section pull-quote-section--accent">
    <div class="container">
      <blockquote class="pull-quote">
        <p>
          "Weemston began as an act of choosing a life built with intention."
        </p>
      </blockquote>
    </div>
  </section>

  <!-- Sailing Images -->
  <section class="dual-image-section">
    <figure class="dual-image">
      <img
        src="/images/about/sailing-gear.webp"
        alt="Life on the boat"
        loading="lazy"
      />
    </figure>
    <figure class="dual-image">
      <img
        src="/images/about/couple-marina.webp"
        alt="Jessica and husband at the marina"
        loading="lazy"
      />
    </figure>
  </section>

  <!-- Relationship & Name -->
  <section class="content-section">
    <div class="container">
      <div class="content-narrow">
        <h3>A Practice Rooted in Relationship</h3>
        <p>
          As we navigated that transition, former colleagues and leaders I had
          partnered with began reaching out with projects that aligned with the
          work I love most. What began as a handful of meaningful engagements
          grew into something larger, shaped by relationships, trust, and the
          opportunity to bring the full range of my strengths into each project.
        </p>

        <h3>The Meaning Behind the Name</h3>
        <p>
          The name Weemston is a small nod to that foundation. Formed by
          blending my last name with my husband's, it reflects the spirit of
          building something with intention, rooted in partnership and shaped by
          a shared vision of a life and career built with care.
        </p>
      </div>
    </div>
  </section>

  <!-- Closing -->
  <section class="closing-section">
    <div class="container">
      <div class="closing-content">
        <h3>A Home for Aligned, Impactful Work</h3>
        <p>
          Over time, the practice that emerged became Weemston Consulting, a
          home for work that feels aligned, meaningful, and deeply human.
        </p>
      </div>
    </div>
  </section>
</div>

<style>
  .layout-editorial {
    --section-spacing: var(--space-3xl);
  }

  .container {
    max-width: var(--container-max-width);
    margin-inline: auto;
    padding-inline: var(--container-padding);
  }

  /* Hero */
  .hero {
    padding-block: var(--space-2xl) var(--section-spacing);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    align-items: start;
  }

  @media (min-width: 768px) {
    .hero-grid {
      grid-template-columns: 320px 1fr;
      gap: var(--space-2xl);
    }
  }

  @media (min-width: 1024px) {
    .hero-grid {
      grid-template-columns: 380px 1fr;
    }
  }

  .hero-image img {
    width: 100%;
    max-width: 380px;
    aspect-ratio: 1;
    object-fit: cover;
    border-radius: var(--border-radius-lg);
  }

  .hero-label {
    display: block;
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-primary);
    margin-bottom: var(--space-sm);
  }

  .hero-content h1 {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-tight);
    color: var(--color-text);
    margin: 0 0 var(--space-lg);
  }

  .hero-intro {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: var(--color-text);
    margin-bottom: var(--space-md);
  }

  .hero-detail {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
  }

  /* Carousel Section */
  .carousel-section {
    overflow: hidden;
    padding: var(--space-lg) 0;
  }

  .carousel-track {
    display: flex;
    gap: var(--space-md);
    padding: 0 var(--container-padding);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .carousel-track::-webkit-scrollbar {
    display: none;
  }

  .carousel-item {
    flex: 0 0 auto;
    width: 300px;
    margin: 0;
    scroll-snap-align: start;
    border-radius: var(--border-radius-md);
    overflow: hidden;
  }

  @media (min-width: 768px) {
    .carousel-item {
      width: 400px;
    }
  }

  .carousel-item img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
  }

  /* Pull Quote */
  .pull-quote-section {
    padding-block: var(--space-2xl);
    background: var(--color-surface);
  }

  .pull-quote-section--accent {
    background: color-mix(in srgb, var(--color-primary) 8%, white);
  }

  .pull-quote {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .pull-quote p {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-style: italic;
    line-height: var(--line-height-normal);
    color: var(--color-text);
    margin: 0;
  }

  /* Content Sections */
  .content-section {
    padding-block: var(--section-spacing);
  }

  .content-section--muted {
    background: var(--color-surface);
  }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  @media (min-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-2xl);
    }

    .content-grid--reverse {
      direction: rtl;
    }

    .content-grid--reverse > * {
      direction: ltr;
    }
  }

  .content-text h2 {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-tight);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .content-text h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-tight);
    color: var(--color-text);
    margin: var(--space-lg) 0 var(--space-sm);
  }

  .content-text h3:first-child {
    margin-top: 0;
  }

  .content-text p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0 0 var(--space-md);
  }

  .content-text p:last-child {
    margin-bottom: 0;
  }

  .content-image img {
    width: 100%;
    border-radius: var(--border-radius-lg);
    object-fit: cover;
  }

  .content-image--accent img {
    aspect-ratio: 4/3;
  }

  .content-image--portrait img {
    aspect-ratio: 4/5;
    max-width: 400px;
  }

  .content-image--sailing img {
    aspect-ratio: 3/4;
    max-width: 350px;
  }

  @media (min-width: 768px) {
    .content-image--portrait img,
    .content-image--sailing img {
      margin-left: auto;
    }
  }

  /* Full Width Image */
  .full-width-image {
    padding: 0 var(--container-padding);
    max-width: 900px;
    margin: var(--space-xl) auto;
  }

  .full-width-image img {
    width: 100%;
    border-radius: var(--border-radius-lg);
    object-fit: cover;
    aspect-ratio: 16/9;
  }

  /* Dual Image Section */
  .dual-image-section {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
    padding: var(--space-xl) var(--container-padding);
    max-width: 1000px;
    margin: 0 auto;
  }

  @media (min-width: 640px) {
    .dual-image-section {
      grid-template-columns: 1fr 1fr;
    }
  }

  .dual-image {
    margin: 0;
    overflow: hidden;
    border-radius: var(--border-radius-md);
  }

  .dual-image img {
    width: 100%;
    aspect-ratio: 4/3;
    object-fit: cover;
    display: block;
  }

  /* Narrow Content */
  .content-narrow {
    max-width: 700px;
    margin-inline: auto;
  }

  .content-narrow h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: var(--space-xl) 0 var(--space-md);
  }

  .content-narrow h3:first-child {
    margin-top: 0;
  }

  .content-narrow p {
    font-size: var(--font-size-base);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
  }

  /* Section Divider */
  .section-divider {
    padding-block: var(--space-xl);
  }

  .section-divider hr {
    border: none;
    height: 1px;
    background: var(--color-border);
    max-width: 200px;
    margin: 0 auto;
  }

  /* Origin Header */
  .origin-header {
    padding-block: var(--space-xl);
    text-align: center;
  }

  .origin-title {
    font-family: var(--font-heading);
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0;
  }

  /* Closing */
  .closing-section {
    padding-block: var(--space-2xl) var(--space-3xl);
    background: var(--color-surface);
    text-align: center;
  }

  .closing-content {
    max-width: 600px;
    margin-inline: auto;
  }

  .closing-content h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    color: var(--color-text);
    margin: 0 0 var(--space-md);
  }

  .closing-content p {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-muted);
    margin: 0;
  }
</style>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia(
    '(prefers-reduced-motion: reduce)'
  ).matches;

  if (!prefersReducedMotion) {
    gsap.from('.hero-image', {
      opacity: 0,
      y: 30,
      duration: 0.8,
      ease: 'power2.out',
    });

    gsap.from('.hero-content > *', {
      opacity: 0,
      y: 20,
      duration: 0.6,
      stagger: 0.1,
      delay: 0.2,
      ease: 'power2.out',
    });

    const contentSections = gsap.utils.toArray(
      '.content-section'
    ) as HTMLElement[];
    contentSections.forEach((section) => {
      const text = section.querySelector('.content-text');
      const image = section.querySelector('.content-image');

      if (text) {
        gsap.from(text.children, {
          scrollTrigger: { trigger: section, start: 'top 80%', once: true },
          opacity: 0,
          y: 30,
          duration: 0.6,
          stagger: 0.1,
          ease: 'power2.out',
        });
      }

      if (image) {
        gsap.from(image, {
          scrollTrigger: { trigger: section, start: 'top 80%', once: true },
          opacity: 0,
          scale: 0.95,
          duration: 0.8,
          ease: 'power2.out',
        });
      }
    });

    const pullQuotes = gsap.utils.toArray('.pull-quote') as HTMLElement[];
    pullQuotes.forEach((quote) => {
      gsap.from(quote, {
        scrollTrigger: { trigger: quote, start: 'top 85%', once: true },
        opacity: 0,
        y: 20,
        duration: 0.8,
        ease: 'power2.out',
      });
    });
  }
</script>
